export enum UserRole {
    Admin = 2,
    NormalUser = 3
}

export enum Gender {
    Male = 1,
    Female = 2,
    Others = 3
}

export enum Status {
    Offline = 0,
    Online = 1,
    Call = 2
}

export enum LanguagePreference {
    English = 0,
    Hindi = 1,
}
